export interface UserData {
  id: number;
  name: string;
  // ... 다른 필요한 사용자 데이터 타입 정의
};

const mockUserData: UserData ={
  id:1,
  name: "seung ah",
  // ... 다른 필요한 사용자 데이터
};

export default mockUserData